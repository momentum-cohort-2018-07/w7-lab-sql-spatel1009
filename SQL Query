Find all time entries: 500

Find the developer who joined most recently: 
Mr. Leopold Carter

Find the number of projects for each client: 
3

Find all time entries, and show each one's client name next to it:
9 
select distinct clients.id, clients.name from projects 
inner join clients on clients.id = projects.client_id
inner join time_entries on time_entries.project_id = projects.id;

Find all developers in the "Ohio sheep" group: 
50

Find the total number of hours worked for each client:
select clients.id, sum(time_entries.duration) from projects 
inner join clients on clients.id = projects.client_id
inner join time_entries on time_entries.project_id = projects.id
group by clients.id;


Find the client for whom Mrs. Lupe Schowalter (the developer) has 
worked the greatest number of hours.
Kuhic-Bartoletti

select clients.name, time_entries.duration from projects 
inner join clients on clients.id = projects.client_id
inner join time_entries on time_entries.project_id = projects.id
inner join developers on developers.id = time_entries.developer_id
where developers.id = 28
order by time_entries.duration desc;

List all client names with their project names (multiple rows for one client is fine).  Make sure that clients still show up even if they have no projects.
42

select projects.name from projects union select clients.name from clients;

Find all developers who have written no comments.
13

select developers.id from developers 
where not exists (select comments.comment from comments where 
comments.developer_id = developers.id);



Find all developers with at least five comments.

Find the developer who worked the fewest hours in January of 2015.

Find all time entries which were created by developers who were 
not assigned to that time entry's project.

Find all developers with no time put towards at least one of their assigned projects.

Find all pairs of developers who are in two or more different groups together.